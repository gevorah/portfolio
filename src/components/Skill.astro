---
interface Props {
  title: string
  icon: any
}

const { title, icon: Icon } = Astro.props
---

<div class="flex flex-col-reverse items-center hover:scale-110 hover:font-medium">
  <h1 class="text-lg mt-1">{title}</h1>
  <Icon class="w-24 h-24 text-primary dark:text-secondary" />
</div>
